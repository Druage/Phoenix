
#include "input-gamepad.h"

/*GamePadEventHandler::GamePadEventHandler() :
    QObject(parent) {

    //timer.setInterval(10);
    //connect(timer, SIGNAL(timeout()), this, processGamePadEvents());
    gamepad = new sf::Joystick();
}

GamePadEventHandler::~GamePadEventHandler() {
    events.clear();
}

GamePadEventHandler::pushEvent(QEvent &event) {
    events.enqueue(event.type());
}

GamePadEventHandler::popEvent(QEvent &event, bool block) {

    if (!events.empty()) {
        top_event = events.dequeue();
        return true;
    }

    return false;

}
*/


